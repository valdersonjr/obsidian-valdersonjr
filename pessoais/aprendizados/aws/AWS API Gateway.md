# AWS API Gateway

API Gateway publica APIs HTTP/REST para clientes, com autenticação, throttling e integração com backends (comum: Lambda).

## Quando usar

- Expor endpoints HTTP para funções/serverless ou microserviços.

## Relacionado

- [[AWS Lambda]]
- [[AWS IAM]] (auth via IAM, policies)
- [[AWS CloudWatch]] (logs/métricas)
- [[AWS Route 53]] (domínio)

